=== TC34 (Bug Bash Row 34): change an EXISTING field's conditional-required condition ===
Destination: actions-twilio-messaging  (id 674f23ece330374dc1ecc874)
Tested by: Harsh Joshi | Date: 7/31/2026 | Status: PASS (clean, isolated run)

WHAT WAS CHANGED IN CODE
  sendMessage/fields.ts -> testConditionalField (an existing conditionally-required field
  already present in CP from TC33). Changed its required condition value:
      channel is 'Messenger'  ->  channel is 'Whatsapp'
  (description updated to match). Regenerated metadata.json via `yarn generate:metadata-payload`,
  pushed to `staging`, then force-pushed via the Retool bot.

FORCE-PUSH BEHAVIOR (the specific thing TC34 tests)
  This is an EXISTING-field-only change. Per the sheet's expectation, a non-force push should
  silently no-op and the change only applies with force:true.
  >> OBSERVED: Force push was the only option — there was no non-force path to exercise the no-op.
     This matches Row 32's earlier finding ("By default the option is force push. No option available
     other than force push."). So the sheet's "non-force silently no-ops" expectation could not be
     tested here; the change applied via force as expected.

DIFF: data1 (pre-push CP state) -> data2 (post-force-push CP state)
Noise filtered out: createdAt/updatedAt timestamps, regenerated field `id`s, `format:null`.

--------------------------------------------------------------------------------
THE TEST SUBJECT — EXPECTED. All 4 deltas are the intended change, nothing else:
--------------------------------------------------------------------------------
[EXPECTED / PASS] fields[testConditionalField].displayMetadata.conditionallyRequired.conditions[channel].value
    - "Messenger"
    + "Whatsapp"
    => The conditionallyRequired hint picked up the new condition value.

[EXPECTED / PASS] fields[testConditionalField].fieldSchema.allOf
    - [{ if: { properties: { channel: { const: "Messenger" } }, required: ["channel"] },
         then: { required: ["testConditionalField"] } }]
    + [{ if: { properties: { channel: { const: "Whatsapp"  } }, required: ["channel"] },
         then: { required: ["testConditionalField"] } }]
    => The JSON-schema allOf/if/then constraint flipped its `const` to the new value too. BOTH the
       schema and the displayMetadata stayed in sync — this is the key correctness check for TC34.

[EXPECTED / PASS] fields[testConditionalField].description                         (Messenger -> Whatsapp wording)
[EXPECTED / PASS] fields[testConditionalField].fieldSchema.properties.testConditionalField.description  (same)
    => Cosmetic description update that accompanied the change.

--------------------------------------------------------------------------------
CONCLUSION
--------------------------------------------------------------------------------
- Changing an existing field's conditional-required condition: WORKS AS EXPECTED. The bot updated
  BOTH fieldSchema.allOf (const Messenger -> Whatsapp) AND displayMetadata.conditionallyRequired,
  keeping them consistent. Flat top-level required stayed false throughout.
- This was a fully ISOLATED run: exactly 4 meaningful deltas, all part of the intended change.
  No auth/region/sendAt drift and no empty-array normalization noise (the earlier TC33 push had
  already synced all of that), which is why this qualifies as a clean PASS rather than Partial.
- Only open item: record the force-push behavior observed in Retool (see "OBSERVED" above) to
  document whether a non-force push no-ops as the sheet expects.

CLEANUP REQUIRED (post bug-bash)
  testConditionalField is a TEMPORARY field added for TC33/TC34. It must be removed from
  sendMessage/fields.ts (and metadata.json regenerated) before staging flows toward production.
